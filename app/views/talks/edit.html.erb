<%= form_for @talk do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title, :size => 30 %> 
    <%= f.label :body %>
    <%= f.text_field :body, :size => 140 %> 
    <%= f.submit "保存する" %>
  <% end %>